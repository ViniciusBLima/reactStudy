import React from "react";

export default props =>{

    return(
        <div>
            <button onClick={props.som}>+</button>
            <button onClick={props.sub}>-</button>
        </div>
    )
}